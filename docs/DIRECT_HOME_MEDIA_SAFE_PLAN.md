# HubSyncBr 0.7.3.6 — Direct Home + Safe MediaMode

Correções:
- Substitui chamadas problemáticas de `hubHomeDataUrl()` por uma constante global segura.
- Remove dependência de `toolbarView` dentro de `setMediaMode(boolean)`.
- Mantém o `mediaMode` ativo para a próxima etapa visual da Media Hub.
