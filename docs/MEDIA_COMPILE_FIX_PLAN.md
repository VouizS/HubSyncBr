# HubSyncBr 0.7.3.4 — Media Compile Fix

Correções:
- Adiciona `mediaMode` dentro de `StreamPane`.
- Adiciona `setMediaMode(boolean)` dentro de `StreamPane`.
- Adiciona `hubHomeDataUrl()` como fallback seguro da homepage.
- Mantém a lógica Media Hub separada, sem mexer na versão estável anterior.
