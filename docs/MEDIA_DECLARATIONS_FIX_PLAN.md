# HubSyncBr 0.7.3.3 — Media Workspace Declarations Fix

Correção aplicada:
- Declara `webGroup`.
- Declara `mediaGroup`.
- Declara `mediaWorkspaceMode`.
- Usa automaticamente o mesmo tipo retornado por `createGroup(...)`.
- Mantém separação entre workspace web/home e workspace Media Hub.
